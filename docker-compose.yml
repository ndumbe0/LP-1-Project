version: '3.8'

services:
  startup-analyzer:
    build: .
    container_name: startup-funding-analyzer
    ports:
      - "127.0.0.1:8501:8501"
    environment:
      - GOOGLE_AI_API_KEY=${GOOGLE_AI_API_KEY:-}
    restart: unless-stopped
